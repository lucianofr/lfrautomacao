{
  "name": "lfrautomacao",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd site && npm install",
    "build": "cd site && npm run build",
    "start": "cd site && npm start"
  }
}
